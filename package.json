{
  "name": "tourrankings",
  "version": "1.0.0",
  "main": "server/index.js",
  "scripts": {
    "scrape": "bun src/scrappers/scrape_proCyclingStats.js",
    "start": "bun server/index.js",
    "build": "bun scripts/build.js",
    "local": "docker compose -f docker-compose.local.yml up",
    "local:cron": "docker compose -f docker-compose.local.yml --profile cron up",
    "local:down": "docker compose -f docker-compose.local.yml down",
    "local:rebuild": "docker compose -f docker-compose.local.yml up --build",
    "local:scrape": "docker compose -f docker-compose.local.yml exec app sh -c '. /chrome-path.sh && bun scrape'",
    "local:shell": "docker compose -f docker-compose.local.yml exec app sh",
    "local:build": "docker compose -f docker-compose.local-build.yml up --build",
    "local:build:clean": "docker compose -f docker-compose.local-build.yml build --no-cache && docker compose -f docker-compose.local-build.yml up",
    "local:build:down": "docker compose -f docker-compose.local-build.yml down",
    "local:build:scrape": "docker compose -f docker-compose.local-build.yml exec app bun scrape",
    "deploy:dev": "fly deploy --config fly.dev.toml",
    "deploy:prod": "fly deploy --config fly.prod.toml",
    "deploy:dev:logs": "fly logs --config fly.dev.toml",
    "deploy:prod:logs": "fly logs --config fly.prod.toml",
    "audit": "yarn audit",
    "lint": "eslint src/**/*.js",
    "test": "bun test",
    "test:watch": "bun test --watch"
  },
  "keywords": [
    "cycling"
  ],
  "author": "Matthew Anderson",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "compression": "^1.8.1",
    "cors": "^2.8.5",
    "csv-parser": "^3.2.0",
    "ejs": "^3.1.10",
    "express": "^4.21.2",
    "express-rate-limit": "^7.5.1",
    "googleapis": "^150.0.1",
    "helmet": "^8.1.0",
    "jsdom": "^26.1.0",
    "puppeteer-core": "^24.30.0",
    "puppeteer-extra": "^3.3.6",
    "puppeteer-extra-plugin-stealth": "^2.11.2"
  },
  "type": "module",
  "devDependencies": {
    "@eslint/js": "^9.39.1",
    "@playwright/test": "^1.56.1",
    "@types/d3": "^7.4.3",
    "@types/express": "^5.0.5",
    "eslint": "^9.39.1",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-jest": "^28.14.0",
    "eslint-plugin-prettier": "^5.5.4",
    "globals": "^16.5.0",
    "yarn": "^1.22.22"
  },
  "engines": {
    "bun": ">=1.2.10",
    "node": ">=20.0.0"
  }
}
